#include <stdio.h>
#include "arlequin.h"


int read_arlequin_file( const char *filename, 
                        int *ploidy,
                        char **genotypes, 
                        int *sample_sizes, 
                        char *groups,
                        char **markers ) 
{
  printf("will try to read '%s'\n", filename);
  return 0;
}
